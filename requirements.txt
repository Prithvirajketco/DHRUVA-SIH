# Core data science
numpy>=1.24.0
pandas>=2.0.0
scikit-learn>=1.3.0
joblib>=1.3.0

# Geospatial (Windows-friendly, no GDAL build needed)
# rasterio pre-built wheel — install with: pip install rasterio --find-links https://github.com/cgohlke/geospatial-wheels/releases
rasterio>=1.3.0
shapely>=2.0.0
pyproj>=3.6.0
# geopandas uses pyogrio as engine (no fiona/GDAL needed)
geopandas>=0.14.0
pyogrio>=0.7.0

# OSM boundary fetch
osmnx>=1.9.0

# ML extras
xgboost>=2.0.0

# NetCDF / raster temporal stacks
xarray>=2023.1.0
rioxarray>=0.15.0
netCDF4>=1.6.0

# Backend
fastapi>=0.104.0
uvicorn[standard]>=0.24.0
python-dotenv>=1.0.0
pyyaml>=6.0.0

# HTTP
requests>=2.31.0

# Console output
rich>=13.0.0
